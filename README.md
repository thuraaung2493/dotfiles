# Dotfiles

Fast Zsh setup with Oh My Zsh, Starship, Ghostty.

## Install on new Mac

```bash
xcode-select --install
git clone https://github.com/yourname/dotfiles.git ~/dotfiles
cd ~/dotfiles
./install.sh
```
